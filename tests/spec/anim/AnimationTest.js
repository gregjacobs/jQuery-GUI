/*global Ext, Y, tests */
tests.unit.ui.anim.add( new Ext.test.Suite( {
                                                 
    name: 'Animation',
	
	
	setUp : function() {
		
	}
	
	
	// -----------------------------
	
	// Test that the 'target' config is required
	
	// Test that the 'to' config or the 'effect' config is required
	

} ) );